# git-training
Training course on Git

1. setup Git
2. setup global config
  - difftool
  - mergetool
  - alias
3. Git overview - Source Control
4. git init, git clone
5. git status, add, commit, log, push, setup remote?
6. 


### Merge Conflicts

can use text editor to resolve differences, will show lines <<<< that are different
git mergetool


